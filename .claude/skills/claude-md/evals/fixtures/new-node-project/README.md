# task-api

REST API for task management. Express + PostgreSQL.

## Setup

```
npm install
cp .env.example .env  # fill in DATABASE_URL
npm run dev
```

## Tests

```
npm test
```

Tests require a running Postgres. See `docker-compose.yml` for a local setup.
