# dotfiles

個人用の各種設定ファイルを管理するリポジトリ。

## セットアップ

```bash
git clone https://github.com/masaka88/dotfiles.git ~/dotfiles
cd ~/dotfiles
./install.sh
```

`install.sh` はこのリポジトリ配下の設定ファイルを、各ツールが参照する場所へシンボリックリンクします。リポジトリ側を編集すればそのまま反映されます。
